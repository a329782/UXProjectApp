import { DisableRoleDirective } from './disable-role.directive';

describe('DisableRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableRoleDirective();
    expect(directive).toBeTruthy();
  });
});
